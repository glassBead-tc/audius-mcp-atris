# Audius API Credentials
AUDIUS_API_KEY=your_api_key_here
AUDIUS_API_SECRET=your_api_secret_here

# Streaming Server Configuration
STREAMING_PORT=3333  # Default port changed to avoid common conflicts
ALLOWED_ORIGINS=*    # Comma-separated list of allowed origins, or * for all origins

# Rate Limiting (defaults if not set)
# RATE_LIMIT_WINDOW=60000    # Time window in milliseconds (1 minute)
# RATE_LIMIT_MAX=60         # Maximum requests per window
